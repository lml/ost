<%# Copyright 2011-2012 Rice University. Licensed under the Affero General Public 
    License version 3 or later.  See the COPYRIGHT file for details. %>

<%= pageHeading "Consent Form: #{@consent_form.name}" %>

<table class="left_heading">
  <tr>
    <th>Content</th>
    <td><%= @consent_form.html.html_safe %></td>
  </tr>
  <tr>
    <th>E-signature Required?</th>
    <td><%= tf_to_yn(@consent_form.esignature_required) %></td>
  </tr>
</table>

<%
  navitem(:can_update?, @consent_form) { link_to "Edit", edit_consent_form_path(@consent_form) }
  navitem(:can_list?, ConsentForm) { link_to "List", consent_forms_path }
%>
