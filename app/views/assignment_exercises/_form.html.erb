<%= form_for(@assignment_exercise) do |f| %>
  <% if @assignment_exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment_exercise.errors.count, "error") %> prohibited this assignment_exercise from being saved:</h2>

      <ul>
      <% @assignment_exercise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :assignment_id %><br />
    <%= f.number_field :assignment_id %>
  </div>
  <div class="field">
    <%= f.label :topic_exercise_id %><br />
    <%= f.number_field :topic_exercise_id %>
  </div>
  <div class="field">
    <%= f.label :number %><br />
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
