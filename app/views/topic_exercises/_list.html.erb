<%= 
  uber_list topic.topic_exercises, nil, 
                                   {
                                     :list_number => "q#{topic.id}",
                                     :remote_buttons => true,
                                     :sort_path => sort_topic_topic_exercises_path(topic), 
                                     :class => 'topic_sub_section'
                                   } do |te|
    te.exercise.url
  end 
%>