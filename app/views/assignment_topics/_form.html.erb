<%= form_for(@assignment_topic) do |f| %>
  <% if @assignment_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment_topic.errors.count, "error") %> prohibited this assignment_topic from being saved:</h2>

      <ul>
      <% @assignment_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :assignment_id %><br />
    <%= f.number_field :assignment_id %>
  </div>
  <div class="field">
    <%= f.label :topic_id %><br />
    <%= f.number_field :topic_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
